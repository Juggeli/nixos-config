Vendored from https://github.com/nicobailon/pi-subagents at bee5ea0 (v0.33.0), package license MIT.

Only `extensions/index.ts` is loaded by this package. Vendored `src/` keeps foreground execution and TUI rendering aligned with upstream behavior. Wrapper exposes fresh-context single/parallel runs and persistent foreground session resume for `explore`, `review`, `researcher`, and `general-purpose` agents.
