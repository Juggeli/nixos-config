Vendored from https://github.com/nicobailon/pi-subagents at bee5ea0 (v0.33.0), package license MIT.

Only `extensions/index.ts` is loaded by this package. The vendored `src/` tree is kept so foreground execution and TUI rendering follow upstream behavior; the wrapper exposes only foreground fresh-context single/parallel runs with the `explore` and `review` agents.
